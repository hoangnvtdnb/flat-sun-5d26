// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

export class SocialPopup {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):SocialPopup {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsSocialPopup(bb:flatbuffers.ByteBuffer, obj?:SocialPopup):SocialPopup {
  return (obj || new SocialPopup()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsSocialPopup(bb:flatbuffers.ByteBuffer, obj?:SocialPopup):SocialPopup {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new SocialPopup()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

version():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

minLevel():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startSocialPopup(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addVersion(builder:flatbuffers.Builder, version:number) {
  builder.addFieldInt32(0, version, 0);
}

static addMinLevel(builder:flatbuffers.Builder, minLevel:number) {
  builder.addFieldInt32(1, minLevel, 0);
}

static endSocialPopup(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createSocialPopup(builder:flatbuffers.Builder, version:number, minLevel:number):flatbuffers.Offset {
  SocialPopup.startSocialPopup(builder);
  SocialPopup.addVersion(builder, version);
  SocialPopup.addMinLevel(builder, minLevel);
  return SocialPopup.endSocialPopup(builder);
}
}
