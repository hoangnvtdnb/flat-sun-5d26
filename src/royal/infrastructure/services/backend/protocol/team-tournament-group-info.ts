// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { TeamTournamentTeam } from '../../../../../royal/infrastructure/services/backend/protocol/team-tournament-team.js';
import { TeamTournamentUser } from '../../../../../royal/infrastructure/services/backend/protocol/team-tournament-user.js';


export class TeamTournamentGroupInfo {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):TeamTournamentGroupInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsTeamTournamentGroupInfo(bb:flatbuffers.ByteBuffer, obj?:TeamTournamentGroupInfo):TeamTournamentGroupInfo {
  return (obj || new TeamTournamentGroupInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsTeamTournamentGroupInfo(bb:flatbuffers.ByteBuffer, obj?:TeamTournamentGroupInfo):TeamTournamentGroupInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new TeamTournamentGroupInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

groupId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

teams(index: number, obj?:TeamTournamentTeam):TeamTournamentTeam|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new TeamTournamentTeam()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

teamsLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

users(index: number, obj?:TeamTournamentUser):TeamTournamentUser|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new TeamTournamentUser()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

usersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

static startTeamTournamentGroupInfo(builder:flatbuffers.Builder) {
  builder.startObject(3);
}

static addGroupId(builder:flatbuffers.Builder, groupId:bigint) {
  builder.addFieldInt64(0, groupId, BigInt('0'));
}

static addTeams(builder:flatbuffers.Builder, teamsOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, teamsOffset, 0);
}

static createTeamsVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startTeamsVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addUsers(builder:flatbuffers.Builder, usersOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, usersOffset, 0);
}

static createUsersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startUsersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static endTeamTournamentGroupInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createTeamTournamentGroupInfo(builder:flatbuffers.Builder, groupId:bigint, teamsOffset:flatbuffers.Offset, usersOffset:flatbuffers.Offset):flatbuffers.Offset {
  TeamTournamentGroupInfo.startTeamTournamentGroupInfo(builder);
  TeamTournamentGroupInfo.addGroupId(builder, groupId);
  TeamTournamentGroupInfo.addTeams(builder, teamsOffset);
  TeamTournamentGroupInfo.addUsers(builder, usersOffset);
  return TeamTournamentGroupInfo.endTeamTournamentGroupInfo(builder);
}
}
