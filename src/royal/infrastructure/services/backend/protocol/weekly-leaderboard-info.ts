// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { WeeklyLeaderboardUser } from '../../../../../royal/infrastructure/services/backend/protocol/weekly-leaderboard-user.js';


export class WeeklyLeaderboardInfo {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):WeeklyLeaderboardInfo {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsWeeklyLeaderboardInfo(bb:flatbuffers.ByteBuffer, obj?:WeeklyLeaderboardInfo):WeeklyLeaderboardInfo {
  return (obj || new WeeklyLeaderboardInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsWeeklyLeaderboardInfo(bb:flatbuffers.ByteBuffer, obj?:WeeklyLeaderboardInfo):WeeklyLeaderboardInfo {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new WeeklyLeaderboardInfo()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

serverEventId():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

remainingTime():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

users(index: number, obj?:WeeklyLeaderboardUser):WeeklyLeaderboardUser|null {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? (obj || new WeeklyLeaderboardUser()).__init(this.bb!.__indirect(this.bb!.__vector(this.bb_pos + offset) + index * 4), this.bb!) : null;
}

usersLength():number {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.__vector_len(this.bb_pos + offset) : 0;
}

isDeleted():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

config():string|null
config(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
config(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

configVersion():number {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

static startWeeklyLeaderboardInfo(builder:flatbuffers.Builder) {
  builder.startObject(6);
}

static addServerEventId(builder:flatbuffers.Builder, serverEventId:number) {
  builder.addFieldInt32(0, serverEventId, 0);
}

static addRemainingTime(builder:flatbuffers.Builder, remainingTime:bigint) {
  builder.addFieldInt64(1, remainingTime, BigInt('0'));
}

static addUsers(builder:flatbuffers.Builder, usersOffset:flatbuffers.Offset) {
  builder.addFieldOffset(2, usersOffset, 0);
}

static createUsersVector(builder:flatbuffers.Builder, data:flatbuffers.Offset[]):flatbuffers.Offset {
  builder.startVector(4, data.length, 4);
  for (let i = data.length - 1; i >= 0; i--) {
    builder.addOffset(data[i]!);
  }
  return builder.endVector();
}

static startUsersVector(builder:flatbuffers.Builder, numElems:number) {
  builder.startVector(4, numElems, 4);
}

static addIsDeleted(builder:flatbuffers.Builder, isDeleted:boolean) {
  builder.addFieldInt8(3, +isDeleted, +false);
}

static addConfig(builder:flatbuffers.Builder, configOffset:flatbuffers.Offset) {
  builder.addFieldOffset(4, configOffset, 0);
}

static addConfigVersion(builder:flatbuffers.Builder, configVersion:number) {
  builder.addFieldInt32(5, configVersion, 0);
}

static endWeeklyLeaderboardInfo(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createWeeklyLeaderboardInfo(builder:flatbuffers.Builder, serverEventId:number, remainingTime:bigint, usersOffset:flatbuffers.Offset, isDeleted:boolean, configOffset:flatbuffers.Offset, configVersion:number):flatbuffers.Offset {
  WeeklyLeaderboardInfo.startWeeklyLeaderboardInfo(builder);
  WeeklyLeaderboardInfo.addServerEventId(builder, serverEventId);
  WeeklyLeaderboardInfo.addRemainingTime(builder, remainingTime);
  WeeklyLeaderboardInfo.addUsers(builder, usersOffset);
  WeeklyLeaderboardInfo.addIsDeleted(builder, isDeleted);
  WeeklyLeaderboardInfo.addConfig(builder, configOffset);
  WeeklyLeaderboardInfo.addConfigVersion(builder, configVersion);
  return WeeklyLeaderboardInfo.endWeeklyLeaderboardInfo(builder);
}
}
