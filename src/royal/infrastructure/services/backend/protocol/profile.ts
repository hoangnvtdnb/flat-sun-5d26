// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { ProfileSetting } from '../../../../../royal/infrastructure/services/backend/protocol/profile-setting.js';


export class Profile {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):Profile {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsProfile(bb:flatbuffers.ByteBuffer, obj?:Profile):Profile {
  return (obj || new Profile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsProfile(bb:flatbuffers.ByteBuffer, obj?:Profile):Profile {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new Profile()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

profileSetting(obj?:ProfileSetting):ProfileSetting|null {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? (obj || new ProfileSetting()).__init(this.bb!.__indirect(this.bb_pos + offset), this.bb!) : null;
}

name():string|null
name(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
name(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

joinDate():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 8);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

level():number {
  const offset = this.bb!.__offset(this.bb_pos, 10);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

crown():number {
  const offset = this.bb!.__offset(this.bb_pos, 12);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

teamId():bigint {
  const offset = this.bb!.__offset(this.bb_pos, 14);
  return offset ? this.bb!.readInt64(this.bb_pos + offset) : BigInt('0');
}

teamName():string|null
teamName(optionalEncoding:flatbuffers.Encoding):string|Uint8Array|null
teamName(optionalEncoding?:any):string|Uint8Array|null {
  const offset = this.bb!.__offset(this.bb_pos, 16);
  return offset ? this.bb!.__string(this.bb_pos + offset, optionalEncoding) : null;
}

teamLogo():number {
  const offset = this.bb!.__offset(this.bb_pos, 18);
  return offset ? this.bb!.readInt32(this.bb_pos + offset) : 0;
}

deprecatedIsGold():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 20);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

hasSpecialNameStyle():boolean {
  const offset = this.bb!.__offset(this.bb_pos, 22);
  return offset ? !!this.bb!.readInt8(this.bb_pos + offset) : false;
}

static startProfile(builder:flatbuffers.Builder) {
  builder.startObject(10);
}

static addProfileSetting(builder:flatbuffers.Builder, profileSettingOffset:flatbuffers.Offset) {
  builder.addFieldOffset(0, profileSettingOffset, 0);
}

static addName(builder:flatbuffers.Builder, nameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, nameOffset, 0);
}

static addJoinDate(builder:flatbuffers.Builder, joinDate:bigint) {
  builder.addFieldInt64(2, joinDate, BigInt('0'));
}

static addLevel(builder:flatbuffers.Builder, level:number) {
  builder.addFieldInt32(3, level, 0);
}

static addCrown(builder:flatbuffers.Builder, crown:number) {
  builder.addFieldInt32(4, crown, 0);
}

static addTeamId(builder:flatbuffers.Builder, teamId:bigint) {
  builder.addFieldInt64(5, teamId, BigInt('0'));
}

static addTeamName(builder:flatbuffers.Builder, teamNameOffset:flatbuffers.Offset) {
  builder.addFieldOffset(6, teamNameOffset, 0);
}

static addTeamLogo(builder:flatbuffers.Builder, teamLogo:number) {
  builder.addFieldInt32(7, teamLogo, 0);
}

static addDeprecatedIsGold(builder:flatbuffers.Builder, deprecatedIsGold:boolean) {
  builder.addFieldInt8(8, +deprecatedIsGold, +false);
}

static addHasSpecialNameStyle(builder:flatbuffers.Builder, hasSpecialNameStyle:boolean) {
  builder.addFieldInt8(9, +hasSpecialNameStyle, +false);
}

static endProfile(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static createProfile(builder:flatbuffers.Builder, profileSettingOffset:flatbuffers.Offset, nameOffset:flatbuffers.Offset, joinDate:bigint, level:number, crown:number, teamId:bigint, teamNameOffset:flatbuffers.Offset, teamLogo:number, deprecatedIsGold:boolean, hasSpecialNameStyle:boolean):flatbuffers.Offset {
  Profile.startProfile(builder);
  Profile.addProfileSetting(builder, profileSettingOffset);
  Profile.addName(builder, nameOffset);
  Profile.addJoinDate(builder, joinDate);
  Profile.addLevel(builder, level);
  Profile.addCrown(builder, crown);
  Profile.addTeamId(builder, teamId);
  Profile.addTeamName(builder, teamNameOffset);
  Profile.addTeamLogo(builder, teamLogo);
  Profile.addDeprecatedIsGold(builder, deprecatedIsGold);
  Profile.addHasSpecialNameStyle(builder, hasSpecialNameStyle);
  return Profile.endProfile(builder);
}
}
